# Include AFTER proxy-analytics.conf (or any snippet that sets proxy_read_timeout 60s).
# nginx uses the last directive in a location block — this overrides the 60s default.
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
