# Admin API base URL (include /v1)
NEXT_PUBLIC_API_URL=http://localhost:3000/v1

# App environment label (local | staging | production)
NEXT_PUBLIC_APP_ENV=local

# Mapbox public token for live map (pk.* only — same token as mobile EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
